import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const VerticalSpace = ({grap = 10}) => {
  return (
    <View style={{height: grap}} />
  )
}

export default VerticalSpace

const styles = StyleSheet.create({})