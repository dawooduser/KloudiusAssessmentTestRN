{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "jsx": "react-native", // or "react-jsx" if using React 17+
    "module": "esnext",
    "target": "esnext",
    "moduleResolution": "node",
  }
}
